import React from 'react';

const App = () => {
    return (
        <div>
            {/* This component is not used as we're using RouterProvider directly in main.jsx */}
        </div>
    );
};

export default App;